(function () {
    'use strict';
    angular.module('app').controller('saldo', function ($scope) {

        $scope.pageTitle = 'Resumo do Saldo';
        $scope.message = 'chamada tela de Resumo do Saldo.';

    });
})();